[project]
authors = [
    {name = 'Kirill "SlowKing" Nagaqcev', email = "slowkinggg@vk.com"},
]
name = "hexlet-code"
version = "0.1.0"
description = "Page Analyzer is a website that analyzes specified pages for SEO suitability, similar to PageSpeed Insights."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "flask>=3.1.2",
    "flask-babel>=4.0.0",
    "gunicorn>=23.0.0",
    "psycopg2-binary>=2.9.11",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
    "validators>=0.35.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
